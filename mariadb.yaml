---
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-secret
  namespace: etudiant04-ns
type: Opaque
data:
  mariadb-root-password: MTIzNA==
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-initdb
  namespace: etudiant04-ns
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS tododb;
    USE tododb;
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mariadb
  namespace: etudiant04-ns
  labels:
    app: mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mariadb
  template:
    metadata:
      labels:
        app: mariadb
    spec:
      containers:
      - name: mariadb
        image: mariadb:10.6
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mariadb-secret
              key: mariadb-root-password
        ports:
        - containerPort: 3306
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        volumeMounts:
        - name: mariadb-storage
          mountPath: /var/lib/mysql
        - name: mariadb-initdb
          mountPath: /docker-entrypoint-initdb.d
      volumes:
      - name: mariadb-storage
        emptyDir: {}
      - name: mariadb-initdb
        configMap:
          name: mariadb-initdb
---
apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: etudiant04-ns
spec:
  selector:
    app: mariadb
  ports:
  - port: 3306
    targetPort: 3306