apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: etudiant04-ns
  labels:
    app: todo-app
spec:
  type: NodePort
  selector:
    app: todo-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30081